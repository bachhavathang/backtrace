"""Evaluation harness for the probabilistic layer.

The deterministic layers are unit-tested in tests/. The adjudicator is a
judgement call over messy text, so it is *evaluated* against a labelled set
rather than asserted on: see run_eval.py.
"""
